create or replace      function  crear_cat_tipo_empresa
(
	in nombre_ varchar(150),
	in descripcion_ text,
	in registrado_por_ int
)
returns table(
	num  integer,
	nombre varchar(150),
	descripcion text
)
    language 'plpgsql'
AS $body$

declare identificador integer = (select case when (select max(num_tipo_empresa) from cat_tipo_empresa) is null
			then 1 else (select max(num_tipo_empresa) from cat_tipo_empresa)+1 end);

begin

insert into cat_tipo_empresa 
(
	num_tipo_empresa ,
	nombre_tipo_empresa ,
	descripcion,
	registrado_por,
	fecha_registro 
)
values
(
	identificador,
	lower(nombre_),
	descripcion_,
	registrado_por_,
	current_date
);
return query  select CE.num_tipo_empresa,
					 CE.nombre_tipo_empresa,
					 CE.descripcion 
					 from cat_tipo_empresa CE where CE.num_tipo_empresa = identificador limit 1;
end;
$body$;
