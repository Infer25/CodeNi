create or replace      function  get_area()
returns table(
	identificador  integer,
	nombre varchar(150)
)
    language 'plpgsql'
AS $body$


begin


return query  select CE.num_area,
					 CE.nombre_area
					 from cat_area CE order by CE.nombre_area DESC;

end;
$body$;
